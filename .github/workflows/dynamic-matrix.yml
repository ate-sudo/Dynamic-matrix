name: "Dynamic matrix"

on: [ push ]

jobs:
  create-matrix:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3
            - name: List tags
              shell: pwsh
              run: | 
                ls
                git tag -l